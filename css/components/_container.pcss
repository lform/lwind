@layer components {
	/* `container` class defaults to max-width of 2xl, add smaller variants below */
	.container-sm,
	.container-md,
	.container-lg,
	.container-xl {
		@apply container;
	}

	.container-sm {
		max-width: theme('screens.sm');
	}

	.container-md {
		max-width: theme('screens.md');
	}

	.container-lg {
		max-width: theme('screens.lg');
	}

	.container-xl {
		max-width: theme('screens.lg');
	}
}
